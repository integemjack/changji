# Shell 脚本一律 LF，不跟着平台走。
#
# **这不是洁癖，是一个真炸过的 bug。** 这台机器 core.autocrlf=true，
# 仓库里存的是 LF，检出到工作区变成 CRLF。`docker build` 拷的是工作区那份，
# 于是镜像里的 entrypoint.sh 第一行成了 `#!/bin/sh\r`，
# 容器起来报的是：
#
#     exec /usr/local/bin/entrypoint.sh: no such file or directory
#
# ——文件明明在，报的却是找不到；找不到的其实是 `/bin/sh\r` 这个解释器。
# 这个错误信息指向的地方和真正的原因差得很远，不知道的话能查很久。
#
# **这一条不只影响 C++ 那份镜像**：老的 Dockerfile（Python 版）拷的是同一个
# entrypoint.sh，在 Windows 上 build 出来的镜像一样是坏的。之前没暴露，
# 大概是因为它没在 Windows 上 build 过。
*.sh text eol=lf

# 同理：这些都是要在 Linux 里被当脚本执行或按行读的
Dockerfile* text eol=lf
*.toml text eol=lf
