{
  "name": "changji-webapp",
  "private": true,
  "version": "0.1.0",
  "description": "场记 Web 界面：Vue 3 前端，打包后嵌进引擎二进制",
  "workspaces": [
    "client"
  ],
  "scripts": {
    "dev": "npm --workspace client run dev",
    "build": "npm --workspace client run build",
    "lint": "npm --workspace client run lint",
    "test": "npm --workspace client run test"
  },
  "engines": {
    "node": ">=22.12.0"
  }
}
