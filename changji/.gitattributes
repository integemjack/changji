# **shell 脚本必须是 LF。**
#
# core.autocrlf = true 会在检出时把 .sh 也换成 CRLF，推到 Linux 上跑
# 第一行就炸：`set: -: invalid option`，接着 `syntax error near
# unexpected token $'{\r'`——报错指向的行和真正的原因毫无关系，
# 而这些脚本本来就只在服务器上跑。2026-09-10 踩过。
*.sh text eol=lf
